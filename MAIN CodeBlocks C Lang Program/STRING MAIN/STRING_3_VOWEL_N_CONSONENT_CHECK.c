#include<stdio.h>
#include <string.h>
int main ()
{
  char a[100];

    printf ("enter:");
    gets (a);

    if (strlen (a)+=a,e,i,o,u)
    {
        printf("%s has vowels", a);
    }
    else {printf("%s has only consonents");}

return 0;
}
