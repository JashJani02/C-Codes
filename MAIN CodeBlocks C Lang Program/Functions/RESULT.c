/* Plan

1. declare 2 variables, from which one will input the number of subjects and the other will be the loop starter
*/










#include <stdio.h>
#include <math.h>
int main()
{

    int subjects,NumberOfSubjects;

    printf("enter number of subjects");
    scanf("%d", &subjects);

    for (subjects=0;NumberOfSubjects>=subjects; subjects++)
    {
        printf("enter marks of suject");
        scanf("%d", &subjects);
    }

    return 0;
}